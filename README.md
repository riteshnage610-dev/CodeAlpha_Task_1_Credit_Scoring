# Task 1 — Credit Scoring Model
Predicts creditworthiness from financial-history features using Logistic Regression, Decision Tree and Random Forest.
Metrics: Accuracy, Precision, Recall, F1-Score and ROC-AUC.
The included dataset is synthetic for reproducibility and educational use.
Run: `pip install -r requirements.txt` then `python credit_scoring_model.py`.
